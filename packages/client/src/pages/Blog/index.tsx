import { Box } from '@mui/material'
import React, { FC } from 'react'
import { BlogList } from '~/components/Blog/BlogList'

const Blog: FC = () => {
  return (
    <Box>
      <BlogList />
    </Box>
  )
}

export default Blog

